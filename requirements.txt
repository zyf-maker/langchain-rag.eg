# RAG项目极简依赖 - Streamlit Cloud专用
# 仅保留绝对必要的依赖，移除可能导致冲突的包
requests==2.30.0
PyPDF2==3.0.1
langchain==0.1.0
langchain_community==0.0.20
streamlit==1.26.0
streamlit_extras==0.3.6
python-dotenv==1.0.0
# 移除可能导致冲突的大型依赖
# 让pip自动解析核心传递依赖，避免版本冲突