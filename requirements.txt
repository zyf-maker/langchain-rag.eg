# 基础工具库：保持稳定兼容
requests==2.32.3
python-dotenv==1.0.1
httpx==0.27.0

# PDF处理：使用日志中确认存在的最高稳定版
PyPDF2==3.0.1  # 恢复为官方可用版本，避免版本不存在错误

# LangChain套件：保持支持numpy 2.x的版本
langchain==0.2.10  # 兼容numpy 2.1.0和Python 3.13
langchain_community==0.2.9  # 与langchain版本同步

# 向量数据库：兼容新版langchain
chromadb==0.5.17  # 适配Python 3.13和langchain 0.2.x

# 数值计算与机器学习
numpy==1.26.4  # 无冲突，保持指定版本
scikit-learn==1.5.1  # 兼容numpy 2.x和Python 3.13

# 前端组件：稳定支持Python 3.13
streamlit==1.40.0
streamlit_extras==0.4.5

# 数据验证：保持不变
pydantic==2.8.0
pydantic-settings==2.1.0

# 图数据库驱动：适配Python 3.13
neo4j==5.23.0
